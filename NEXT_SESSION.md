# Следующая сессия — media-agents

> Хендофф от сессии 09.07.2026. **Пайплайн запущен, первый боевой батч (9 статей) опубликован и подтверждён живым на сайте `1screen.ru`.** Полный цикл (сбор → PR → merge → деплой на Timeweb → показ) замкнут. Ниже: что сделано, что осталось, и с чего начать.

## 🆕 Сессия 19.07.2026: База знаний (Spravochnik) — новый пайплайн

> **Новый раздел сайта + отдельный пайплайн агентов** для evergreen-справочника терминов/
> компаний/технологий/организаций adtech (`/spravochnik/`). Независим от новостного пайплайна.
> Исходное ТЗ владельца сверено с кодом и переписано → **`для_кодинга/ТЗ_База_знаний.md` (v2)**.
> Ключевые правки против оригинала (раздел 0 ТЗ): JSON-LD генерится в Astro, а не пишется LLM
> во front-matter; добавлена сноска РКН (Meta и т.п.); Wikipedia с normalize+timeout;
> related — по живой коллекции сайта, а не `published.jsonl`; cron → workflow Actions;
> rules-QA уровень 1; переиспользование `git_publisher`/`media_repo`.
>
> **Фаза 1 (backend, media-agents)** — реализуется в этой сессии (`src/spravochnik/`).
> **Фаза 2 (site, media)** — отдельный PR, требует `add_repo media` (schema/роуты/layout/JSON-LD/nav/виджет).
> **Фаза 3** — авто-линкер новость→термин (SEO-мультипликатор).
> Статус реализации — в конце этого файла (секция «Реализация Базы знаний»).

## 🎯 Приоритетный план (утверждён 12.07.2026 — с этого начинать)

Порядок работ на следующие сессии. Детали по каждой задаче — в секциях ниже (по номерам старых «Задач»).

| Ранг | Задача | Тип | Суть | Где чинить | Зависимости |
|------|--------|-----|------|-----------|-------------|
| ~~**1**~~ ✅ | ~~**Персист состояния** (`Commit data state` 403)~~ | Инфра-баг | **ЗАКРЫТО 13.07.2026 (PR #16).** Добавлен `permissions: contents: write` в оба воркфлоу — блока достаточно, настройку репо менять не пришлось. Проверено: digest run 13.07 — «Commit data state» зелёный, push без 403 | `permissions: contents: write` в воркфлоу | Разблокировал №2, №3, №4 |
| ~~**2**~~ ✅ Фаза 1 | **Дедупликация** (ТЗ приложено) | Улучшение | **Фаза 1 СМЁРЖЕНА + проверена боем (run #28, 17.07):** TF-IDF (scikit-learn, символьные n-граммы) заменил текстовый фолбэк; слияние источников в батче → `additional_sources` виден во front-matter (+ схема сайта PR #50); `failed_drafts.jsonl`; тонкий `ArticleStore`. Бой: 3 корректные склейки вкл. перефразировки, QA 11/11. **Осталось:** update-flow → **Фаза 2** (бэклог ниже) | `filter_dedup.py`, `tfidf_engine.py`, `store.py`, `news_writer.py`, `qa.py` | Кросс-прогонный дедуп на персисте `seen_urls` (№1 закрыт) |
| ~~**3**~~ ✅ | **Дайджест недели на материалах сайта** | Фича | **ГОТОВО + ПОДТВЕРЖДЕНО БОЕМ 18.07.2026 (run #4 → PR media#53); PR media-agents#25.** Источник новостей дайджеста переключён с `published.jsonl` на живую коллекцию сайта (`media-site/src/content/news/*.md` в клоне `media`) → битых ссылок больше нет (slug = имя файла = роут). Клон-хелпер → `media_repo.py`. По ходу боя починены 3 дайджест-дефекта: планка заголовка 50–100 (`TITLE_LIMITS`), slug дайджеста = неделя, литеральные `{}` в ссылках (промпт). Тесты 91 зелёный | `digest_writer.py`, `media_repo.py` (новый), `git_publisher.py`, `enricher.py`, `qa.py`, `digest_writer.md` | Дедуп (№2) сделан; `published.jsonl` ≠ «на сайте» → берём коллекцию `media` |
| **4** 🟠→частично | **Качество текстов / «ИИ-голос»** (объединяет старые «Задачи» №2 + №3) | Качество | **Смёржено (эта сессия, см. секцию ниже):** гибкий промпт писателя + хирургическая петля ревизии по QA-замечаниям + тест-режим воркфлоу. Тест-прогон владельца: «стало немного лучше». Точечные дефекты чинятся, диффузный ИИ-голос — нет | `news_writer.md`, `reviser.md`, `qa_style.md`, `stage_write`, `pre_filter/collector` (тест-режим) | Проверено тест-прогоном |
| **4a** 🔴 **ГЛАВНОЕ ДАЛЬШЕ** | **Образцы качественных формулировок для писателя (few-shot)** | Качество | Диффузный ИИ-голос (весь текст гладкий, ни одной фразы не ткнёшь) лечится НЕ запретами, а показом «как надо». Скурировать эталонные формулировки/абзацы/статьи и вшить в промпт писателя как якорь стиля — поднять качество **первого прохода** (не зависеть от петли ревизии) | `news_writer.md` (+ возможно отдельный файл примеров); кандидаты — из репо `media`, курирует **владелец** | Курирование эталонов владельцем |
| ~~**5**~~ ✅ | ~~Двоеточие в заголовке ломает YAML front-matter~~ | Баг | **ЗАКРЫТО 13.07.2026.** `split_front_matter` на ошибке YAML чинит незакавыченные строковые поля (`title`/`description`/`social_title`/`pubDate`) и парсит повторно — материал больше не теряется | `_repair_front_matter` в `frontmatter.py` | Дёшево |
| ~~**6**~~ ✅ | ~~Длина заголовка вне 50–80~~ | Баг | **ЗАКРЫТО 13.07.2026.** `write_news` при промахе длины делает один авто-ретрай с корректирующим хинтом; промпт усилен («СТРОГО 50–80») | `news_writer.write_news` + `news_writer.md` | Дёшево, помогает №4 |
| 7 🟡 | Сверка «отправлено ↔ на сайте» | Улучшение | Автопроверка доезда материалов на сайт | Скрипт: `published.jsonl` ↔ `media/.../news/` | Зависит от №1 |
| 8 ⚪ | RU-фид про мировое событие → ошибочный `adtech-ru` | Edge-case | Гео берётся по фиду, а не по содержанию | Гео-классификация по содержанию | Косметика |
| ~~**Asia**~~ ✅ СМЁРЖЕНО (PR #27) | **Азиатские источники (Alizila) → Редакция новостей Азия** | Фича | Alibaba-экосистема (`region: asia` → `adtech-asia`/`news-asia`/`АЗИЯ`). Реализованы явные `asia`-ветки в pre_filter (выбор словаря по региону)/enricher/news_writer + `keywords_asia.yaml` + подсекция дайджеста. ⚠️ **Боем не подтверждено** — прогон запускали, результат (URL фида Alizila, объём, разметка) не проверен → на следующий прогон | `sources.yaml`, `keywords_asia.yaml`, `pre_filter.py`, `enricher.py`, `news_writer.py`, `digest_writer.md` | Дедуп (№2) сделан |
| — ✅ | `pubDate` = дата выхода на 1screen | Done (10.07) | `news_writer._finalize_meta` | — | — |

**Последовательность:** №1 (персист) — фундамент: №2, №3, №4 упираются в него. Сначала включаем сохранение состояния (дёшево: блок `permissions` + настройка репо), затем заходим в дедуп/дайджест/обучение на «живом» состоянии. Дедуп *внутри одного прогона* можно писать и без персиста, но кросс-прогонная память без него не работает.

## Сессия 17.07.2026: дедупликация — Фаза 1 (TF-IDF + слияние в батче + failed_drafts + ArticleStore) — СМЁРЖЕНО + ПРОВЕРЕНО БОЕМ

**Статус: смёржено (PR #22 `media-agents`, PR #50 `media`) и подтверждено прогоном #28.**
Сделано по ТЗ дедупа (Фаза 1, один репо + кросс-репо схема, низкий риск):

- **TF-IDF дедуп** (`src/utils/tfidf_engine.py`, `scikit-learn`) — активная замена
  текстового фолбэка по заголовкам. Символьные n-граммы (`char_wb`, 3–5) устойчивы
  к русской словоизменительности без внешнего стеммера; IDF-взвешивание плотнее
  Jaccard+SequenceMatcher, ловит перефразировки одного сюжета из разных фидов.
  **Ветка эмбеддингов оставлена ДРЕМАТЬ** (`_embed_items`) — оживёт сама при смене
  провайдера. Индекс пересобирается каждый прогон (корпус маленький).
- **`filter_dedup.py`** — `_text_fallback_events` → `_tfidf_events`: (1) отсев
  дублей опубликованного по заголовку (cross-similarity против корпуса published);
  (2) жадная кластеризация свежих по заголовок+summary в события. **Пороги
  эмпирические**, env-override без редеплоя: `DEDUP_DUP_THRESHOLD` (0.62),
  `DEDUP_CLUSTER_THRESHOLD` (0.45) — докрутить по боевому прогону.
- **Многоисточниковое слияние → `additional_sources` виден.** `news_writer._finalize_meta`
  ДЕТЕРМИНИРОВАННО (из сырья, не из фантазии модели) пишет доп. источники во
  front-matter при >1 источника; один источник → поля нет. Причина: без видимого
  списка читатель видит в тексте факты сверх единственной ссылки → воспринимает
  как галлюцинацию агента. QA валидирует структуру (list of {title, url http(s)},
  не дублирует primary) — иначе строгая схема Astro уронит сборку. Паблишер пишет
  `additional_sources` + `source_count` в `published.jsonl`.
- **`failed_drafts.jsonl`** (`data/published/`, коммитится воркфлоу) — QA при FAIL
  дописывает `{slug, title, reasons, llm_issues, body}` через `ArticleStore`.
  Закрывает «корпус брака» (старая задача 2): размеченное «не прошло + почему»
  переживает эфемерный раннер.
- **`ArticleStore`** (`src/utils/store.py`) — тонкий фасад над `StateManager`
  (композиция + `__getattr__`-делегирование, drop-in по утиной типизации). Новая
  персистентность (`add_failed_draft`/`load_failed_drafts`) — своими методами; шов
  под будущий PostgreSQL (Selectel). `run_pipeline` конструирует `ArticleStore`.
- **Схема сайта (PR #50 в `media`, смёржен):** `content.config.ts` — optional
  `additional_sources` + `updatedDate`; `article/[...slug].astro` рендерит источники
  (лейбл «Источники:» мн. при доп.), JSON-LD `dateModified = updatedDate ?? pubDate`;
  `.source-box { flex-wrap: wrap }`. `astro build` зелёный (121 страница), multi-source
  рендер проверен на тест-материале. Во `frontmatter.FIELD_ORDER` (агенты) заведены
  оба поля (`updatedDate` — задел под update-flow Фазы 2).
- **Тесты:** `tests/test_tfidf.py` (движок, кластеризация, отсев дубля, ArticleStore),
  `tests/test_qa.py` (+валидация additional_sources), `tests/test_content_feedback.py`
  (+эмиссия additional_sources). 81 тест зелёный локально (кроме `test_collector`/
  `test_test_mode` — не собираются: `feedparser`→`sgmllib3k` не билдится в песочнице,
  к правкам не относится).

### ✅ Проверка боем — run #28 (17.07, тест-режим, `main`)

Воронка: 95 собрано → 33 keyword → топ-15 (кап теста) → 14 переведено → 1 отброшен
по релевантности → **11 curated событий** → 11 черновиков → **QA 11/11 PASS**, $0.03, 10 мин.
Эмбеддинги ожидаемо 400 → TF-IDF. **3 корректные склейки:**
- Netflix «должен решить свою проблему…» ↔ «стоит решить проблему…» — **0.86** (перефразировка!);
- CIMM «создаёт **целевую** группу» ↔ «создаёт **рабочую** группу» — **0.87** (перефразировка!);
- Microsoft (тот же сюжет из двух фидов) — **0.86**.

Netflix/CIMM — именно то, что старый текстовый фолбэк пропускал. **Ложных склеек нет**,
7+ различных сюжетов остались раздельными. QA прошли все 11 (включая 3 multi-source) →
`additional_sources` записаны структурно валидно (иначе QA бы завернул). `failed_drafts.jsonl`
в этот прогон пуст (0 брака) — персист не проверился боем, но покрыт тестом.

**Пороги оставлены (`0.62`/`0.45`):** все склейки легли на 0.86–0.87, серую зону
0.45–0.85 ни один случай не задел → строгой калибровки порога ещё нет; докрутка env
(`DEDUP_DUP_THRESHOLD`/`DEDUP_CLUSTER_THRESHOLD`) по первому пограничному случаю на бою.
⚠️ Мелочь на будущий прогон: арифметика воронки сошлась не идеально (14−1−3 склейки ≈ 10,
а curated=11) — вероятно item без заголовка/edge-case счётчика; на поведение дедупа не влияет.

## Сессия 18.07.2026: дайджест на материалах сайта (задача №3) — ПОДТВЕРЖДЕНО БОЕМ (PR media#53)

**Статус: реализовано по ТЗ ниже; end-to-end ПОДТВЕРЖДЕНО БОЕМ (run #4 → PR media#53);
по ходу боя вскрыты и починены 3 дайджест-специфичных дефекта. Ветка → PR media-agents#25.**

### Боевой прогон #3 (18.07, ветка, `workflow_dispatch`)

- ✅ **Ядро задачи работает:** `_news_for_week_from_site` поднял клон `media`, прочитал
  живую коллекцию сайта и собрал дайджест за 2026-W29 — **«написан: 27 новостей, 9238
  знаков»**. Источник истины = сайт подтверждён боем.
- ❌ Прогон упал НЕ на источнике, а на QA самого дайджеста — вскрылись 2 дефекта,
  которых раньше не видели (это, похоже, первый доезд агентского дайджеста до QA;
  `2026-w27.md` на сайте — **ручной** дайджест владельца, автор `y-sokolov`):
  1. **Заголовок 82 > 80.** QA применял новостную планку 50–80 ко всем типам, у
     `digest_writer` нет авто-ретрая (в отличие от `news_writer`, задача 6).
     **Фикс:** `TITLE_LIMITS` в `qa.py` — у дайджеста **50–100** (заголовок суммирует
     несколько сюжетов; схема сайта длину `title` не ограничивает — решение владельца).
     Промпт `digest_writer.md` целит в 50–100 + даёт пример-эталон.
  2. **Slug дайджеста ломался.** Enricher переименовывал дайджест в `{news-slug}.md`
     (`paypal-…`) → URL был бы `/digest/paypal-…` вместо `2026-w29`. Эталон `media`:
     файл = `{week}.md`. **Фикс:** `enrich_draft` для `article_type=="digest"` ставит
     `slug = meta["week"].lower()` (без `ensure_unique` — неделя уникальна, повторный
     прогон перезаписывает). Маршрут: коллекция `digest`, `/digest/<week>`.
- Тесты: `tests/test_qa.py` (+2: 82 — PASS у дайджеста / FAIL у новости; >100 — FAIL),
  `tests/test_digest.py` (+1: slug дайджеста = неделя, LLM-обогащение мокнуто). 91 зелёный.

### Боевой прогон #4 (18.07, ветка, после фиксов) — ✅ END-TO-END, PR media#53

- ✅ Дайджест **дошёл до PR:** «написан: 27 новостей, 8489 знаков» → `enriched: 2026-w29.md
  (category=adtech-world, author=news-world)` → **PR создан: [media#53](https://github.com/yuriysklv-hue/media/pull/53)**.
- ✅ Фикс 1 (заголовок): «PayPal доказывает retail media, а Германия приравнивает AI-обзоры
  к СМИ» — прошёл QA (**было 82>80 FAIL, стало PASS**).
- ✅ Фикс 2 (slug): файл `2026-w29.md`, маршрут `/digest/2026-w29` (был бы `paypal-…`).
- ✅ Контент: 4 секции, RU-подсекция (АКАР), 27 источников, ссылки на живые slug'и сайта.
- ⚠️ **Красный статус прогона = только housekeeping** («Commit data state» → `git pull
  --rebase origin main` add/add-конфликт на фиче-ветке). Идёт ПОСЛЕ создания PR, дайджест
  не ломает — ровно как задокументировано. Сам пайплайн отработал.
- 🔧 **Фикс 3 (по бою #4): ссылки с литеральными `{}`.** Модель скопировала плейсхолдер
  `[{title}](/article/{slug})` буквально → каждая ссылка вышла `[{Заголовок}](…)` с видимыми
  фигурными скобками. Промпт `digest_writer.md` больше не показывает `{}`-плейсхолдеры:
  добавлено ПРАВИЛО ССЫЛОК с конкретным примером и запретом скобок; slug берётся из поля
  Slug списка. Только промпт (LLM, локально не тестируется) → проверится следующим прогоном.
  ⚠️ В уже созданном PR media#53 скобки ещё есть (файл сгенерён до фикса) — владелец правит
  руками (`[{`→`[`, `}]`→`]`) перед мержем ЛИБО перегенерит новым прогоном.

### Правки по разбору боевого дайджеста (7/10) — СМЁРЖЕНО, на сайте (PR #28/#29)

Первый боевой дайджест владелец оценил на 7/10. По разбору:

1. **Убран блок «Что это значит для рынка»** (`digest_writer.md`) — обобщающие выводы
   слабые («нейробизнеслоп»). Дайджест заканчивается на «Кратко», наблюдения — точечно
   в разделах 1–2. Объём 700–1300 слов.
2. **Комплаенс: сноска о запрете Meta/FB/Instagram в РФ.** Дайджест не прогонялся через
   `legal.add_restricted_org_footnotes` → упоминания шли без пометки, в отличие от статей
   (**прямой риск блокировки РКН**). Теперь `write_digest` ставит сноску детерминированно
   (как `news_writer`); промпт просит не помечать эти организации вручную.
3. **QA: переиздание дайджеста той же недели.** Перевыпуск 2026-W29 упал на
   `slug «2026-w29» уже занят` — прошлый выпуск записал slug в `published.jsonl`, а проверка
   дублей slug била по всем типам. Для дайджеста `slug=неделя` стабилен, переиздание легально
   → проверка дублей теперь только для статей (`article_type != "digest"`, `qa.py`).
4. **Расписание** дайджеста перенесено на **сб 20:00 МСК** (было вс 21:00) — PR готов в
   субботу вечером, публикация утром в вс (из-за задержек GitHub вс-PR виден к понедельнику).

Тесты: `test_qa.py`/`test_digest.py` (+сноска-гард, +slug-переиздание, +планка 50–100). Перевыпуск
2026-W29 дошёл до сайта. ⚠️ **Диффузный ИИ-голос дайджеста петля/правки не лечат** — если качество
выводов/формулировок всё ещё слабое, рычаг тот же, что для статей: few-shot образцы (задача 4a).

### ТЗ (реализация в прошлой части сессии)

**Статус: реализовано по ТЗ ниже, тесты зелёные локально.**

- **`src/publishers/media_repo.py` (новый)** — общий доступ к репо `media`: `_git_env()`,
  `_prepare_clone()`, `CLONE_DIR`, `clone_or_update_media() -> Path`,
  `news_dir(clone) -> Path`. `git_publisher.py` теперь **импортирует** эти хелперы
  (локальные определения удалены, `_git_env`/`_prepare_clone` реэкспортятся через
  namespace публишера — обратная совместимость сохранена, `CLONE_DIR` делится).
- **`digest_writer._news_for_week` → `_news_for_week_from_site(week)`** — читает живую
  коллекцию `news_dir()` (`.md` в клоне `media` на свежем `main`), а НЕ `published.jsonl`:
  slug = `path.stem` (= роут `/article/<slug>`), фильтр по date-части `pubDate` в границах
  `week_bounds` включительно, запись `{slug, title, category, geo, source_name, source_url,
  pub_date}`, сортировка по `pub_date`. Клон не поднялся → исключение наверх (`run_digest`
  ловит → `return 1`, дайджест не выпущен). Нет папки/статей → `[]` → `write_digest` → `None`.
- **`published.jsonl` из дайджеста убран** (остаётся под сверку «отправлено ↔ на сайте»,
  задача №7). `write_digest`/`run_digest` сигнатуры не менялись (`state` — только для LLM).
- **Тесты `tests/test_digest.py` (7, без сети):** границы недели; выборка по `pubDate` +
  slug из имени файла + сортировка; сохранение `category`/`geo` (RU-подсекция); папка
  `digest/` не подхватывается в выборку новостей; пустая неделя → `None`; отсутствие
  `news_dir` → `[]`; структура `_news_list_block`. Клон мокается на tmp-папку.
- ✅ **Проверено боем (run #4, см. выше)** — дайджест дошёл до PR media#53. Трейд-офф
  «источник истины = сайт» в силе: дайджест собирается только из смёрженного за неделю;
  поздние несмёрженные материалы недели уедут в следующий дайджест.

## ТЗ (реализовано 18.07.2026 — см. секцию выше): Дайджест недели на материалах сайта (задача №3)

> Решение владельца (18.07.2026). ТЗ ниже реализовано в сессии 18.07 — оставлено как эталон.

### Проблема и решение

Дайджест **уже не пересобирается из RSS** — `digest_writer._news_for_week` читает
`published.jsonl` (`state.load_published()`) и фильтрует по неделе. Но `published.jsonl`
= «**отправлено в PR**», а НЕ «**живёт на сайте**»:
- `pr_merged` пишется `False` в `git_publisher._record_published` и **никогда не
  обновляется** в `True` (проверено: единственное вхождение — запись `False`);
- владелец **вручную удалял** часть материалов из PR перед мержем (дубли и др.) →
  такие статьи остаются в `published.jsonl`, но на сайте их нет → дайджест дал бы
  **битые ссылки** (`/article/{slug}` в никуда).

**Решение — источник истины = реальная коллекция сайта в репо `media`**
(`media-site/src/content/news/*.md`, только то, что смёржено и живёт). `published.jsonl`
из дайджеста **убрать**. **Фолбэка НЕТ:** если клон `media` не поднялся или за неделю
нет статей — дайджест НЕ выпускать (падать/None). Логика владельца: *лучше нет
дайджеста, чем дайджест с битыми ссылками*. Локальных прогонов дайджеста не будет,
поэтому городить откат на `published.jsonl` не нужно.

`published.jsonl` НЕ трогаем — он остаётся под сверку «отправлено ↔ на сайте» (задача №7).

### Почему это реально (доступ к `media` из `media-agents` уже есть)

- `digest.yml` уже прокидывает в `run_digest.py` секреты `GH_TOKEN` (PAT на `media`),
  `MEDIA_REPO`, `MEDIA_SITE_SUBDIR` — те же, что у паблишера.
- `git_publisher._prepare_clone(git_url)` уже клонирует/обновляет `media` в
  `CLONE_DIR = ROOT/media-clone` (main, depth 1), а `_git_env()` собирает
  авторизованный URL из `GH_TOKEN`+`MEDIA_REPO`. Паблишер уже читает/пишет по пути
  `CLONE_DIR/{MEDIA_SITE_SUBDIR}/src/content/{news|digest}/` — **ровно та папка**,
  где лежат живые новости. Дайджест читает тот же клон, просто раньше и на чтение.

### План реализации

1. **Общий хелпер клона `media`** — вынести `_git_env()` + `_prepare_clone()` из
   `src/publishers/git_publisher.py` в новый модуль `src/publishers/media_repo.py`
   (паблишер импортирует оттуда, поведение НЕ менять — обратная совместимость).
   Экспорт:
   - `clone_or_update_media() -> Path` — путь к клону `media` на свежем `main`;
   - `news_dir(clone: Path) -> Path` = `clone / MEDIA_SITE_SUBDIR / "src/content/news"`.
2. **`src/writers/digest_writer.py`:**
   - Заменить `_news_for_week(state, week)` → `_news_for_week_from_site(week)`:
     - `clone_or_update_media()` → прочитать все `.md` в `news_dir`;
     - для каждого: `split_front_matter` (уже есть), **slug = имя файла** (`path.stem`);
     - фильтр: `pubDate` (date-часть) в границах `week_bounds(week)` включительно;
     - собрать запись `{slug, title, category, geo, source_name(=source.title),
       source_url(=source.url), pub_date}`; сортировать по `pub_date`.
     - Клон не поднялся → пробросить исключение (НЕ глушить). Пустая неделя → `None`
       (как сейчас — дайджест не пишем).
   - `write_digest`: убрать зависимость от `state` для источника новостей (клиент LLM
     всё ещё берётся через `state`); `_news_list_block` оставить как есть (поля те же:
     title/slug/category/source/pubDate).
3. **`run_digest.py`** — сигнатуры те же; исключение клона ловится существующим
   `try/except` вокруг → лог + `return 1` (дайджест не выпущен).
4. **Ссылки** — промпт `digest_writer.md` уже строит `[{title}](/article/{slug})`,
   что совпадает с роутингом сайта (`media-site/src/pages/article/[...slug].astro`
   → `/article/<slug>/`). Так как slug = имя файла из коллекции, ссылки **гарантированно
   валидны**. Формат менять не нужно.
5. **RU-секция** дайджеста (подсекция «Россия» при `Category: adtech-ru`) — `category`
   читается из front-matter `.md`, работает без изменений.
6. **Тесты** (`tests/test_digest.py`):
   - `week_bounds`/`iso_week` — границы недели (пн–вс включительно);
   - выборка из набора фейковых `.md` (мокнуть `clone_or_update_media` на tmp-папку с
     подготовленными файлами — БЕЗ сети): попадание/непопадание по `pubDate`, slug из
     имени файла, сортировка;
   - пустая неделя → `None`;
   - структура `_news_list_block`.

### Подводные камни

- **Даты:** `pubDate` в site-`.md` — ISO c временем UTC (из `news_writer._finalize_meta`
  = `utcnow_iso()`). Сравнивать по **date-части** с `week_bounds` (границы включительно).
- **Порядок клона в дайджест-флоу:** `write_digest` поднимает клон `main` ДО
  `enrich→qa→publish`; затем `publish` переиспользует тот же `CLONE_DIR`
  (`_prepare_clone`: fetch+reset, потом `checkout -B digest-<week>`). Конфликта нет,
  но проверить, что `write_digest` не оставляет `CLONE_DIR` в грязном состоянии
  (только читает — не пишет).
- **Пустая коллекция/новый репо:** `news_dir` может не существовать → трактовать как
  «нет новостей за неделю» (None), не падать на отсутствии папки.
- **Тест-режим:** у `digest.yml` нет `test_mode`; при желании прогнать безопасно —
  добавить dry-run-ветку отдельно (вне этого ТЗ).

### Файлы

`src/writers/digest_writer.py` (переработка источника), `src/publishers/media_repo.py`
(новый, общий клон-хелпер), `src/publishers/git_publisher.py` (импорт хелпера вместо
локальных `_git_env`/`_prepare_clone`), `run_digest.py` (без изменений логики),
`tests/test_digest.py` (новый).

## ТЗ: Азиатские источники (Alizila) — Редакция новостей Азия

> Владелец собрал ТЗ (18.07.2026), сверено с кодом и переписано под реальный пайплайн.
> Цель: подключить азиатский рынок (Alibaba и экосистема) и публиковать от **`news-asia`**
> («Редакция новостей Азия»), по аналогии с Мир (`news-world`) и Россия (`news-ru`).

### Контекст и стратегия

Азиатский рынок (~40% глобального digital ad spend) в ленте не представлен. Работаем
напрямую с первоисточниками. Из ~25 платформ RSS с полным текстом нашёлся один — **Alizila**
(официальный ньюсрум Alibaba Group, англ., без paywall, RSS 2.0). Покрывает Alibaba, Taobao,
Tmall, AliExpress, Amap, Lazada, Cainiao, Ele.me. Для первой итерации — достаточно; других
источников не подключаем. Зависимость (дедуп/мультисорс, задача №2) — закрыта 17.07.

### ⚠️ Главная поправка к исходному ТЗ (сверка с кодом)

Исходное ТЗ местами оптимистично: «маппинги заложены / без изменений». По факту **детерминированное
ветвление региона знает только `ru` и `world`; `asia` молча уходит в `world`**. Что реально надо:

1. **Pre-Filter выбирает словарь по `language`, НЕ по `region`** (`pre_filter.py:97,100`:
   `{"en": keywords, "ru": keywords_ru}[item.language]`). Alizila `language: en` → без правки
   попадёт на английский `keywords.yaml`, а не на `keywords_asia.yaml`. **Нужна ветка по региону:**
   `region=="asia" → keywords_asia`, иначе прежний выбор по языку.
2. **Enricher** (`enrich_draft`) ветвит `ru` vs `world`: `default_category`, `default_geo`, `author`
   (стр. 115–116, 149, 166–169) + `_region_hint` (стр. 58–65). **Нужна явная ветка `asia`:**
   `adtech-asia` / geo `["АЗИЯ"]` / author `news-asia` + asia-хинт для LLM.
3. **News Writer** (`news_writer._finalize_meta:81-83`) сеет `category`/`geo` по региону тем же
   `ru`-vs-`world`. **Нужна ветка `asia`** (симметрично enricher). Исходное ТЗ 4.6 «Author News —
   без изменений» здесь **неверно**.

### Что уже готово (блокеров нет)

- ✅ **Автор `news-asia` есть в репо `media`** (`media-site/src/content/authors/news-asia.md`) →
  строгая схема Astro (`author: reference('authors')`) сборку не уронит.
- ✅ `config/vocabulary.yaml` содержит `adtech-asia`; `config/authors.yaml` — маппинг
  `adtech-asia → news-asia`; гео `АЗИЯ` валидно в схеме сайта.
- ✅ **Коллектор уже шлёт User-Agent** (`rss_collector.py:75`) → 403 от Alizila не будет.
- ✅ Коллектор берёт `language`/`region` из `sources.yaml` (стр. 163–164) — добавление фида
  собирается без правок кода. Translator (en→ru), Dedup (TF-IDF по русскому), QA, Publisher — без изменений.

### Кросс-региональное слияние — сознательное правило

Событие берёт `region` от **primary-источника** (`filter_dedup._make_event:182`). Если западный
(world) и Alizila (asia) пишут про одно событие и primary — западный, статья выйдет `adtech-world`.
Для чисто-алибабовских новостей западного дубля обычно нет → останется `asia`. **Решение для
первой итерации: оставляем region=primary (как сейчас), не усложняем.** Это родственник RU
edge-case (задача 8). Если на бою азиатские сюжеты будут массово «утекать» в world — вводим
правило «asia, если среди источников есть asia» отдельной задачей.

### План реализации (файлы)

1. **`config/sources.yaml`** — добавить Alizila: `language: en`, `region: asia`,
   **`max_age_days: 30`** (корпоративный ньюсрум → архивный хвост, урок RU run #13).
2. **`config/keywords_asia.yaml`** (новый) — зеркало `keywords_ru.yaml` по структуре
   (те же уровни/веса/exclusions/override, `src/utils/scoring.py`), но словарь под монетизацию:
   платформы (Alibaba, Taobao, Tmall, AliExpress, Lazada, Cainiao, ByteDance, Tencent, JD.com,
   Pinduoduo, Shopee, Amap…), commerce/monetization-термины (GMV, take rate, loyalty, membership,
   marketplace, live commerce…). **`score_threshold: 15`** (1 источник на всю Азию: запас для
   статей с одним платформенным попаданием). Глобальный `keywords.yaml` **НЕ трогаем**.
3. **`src/processors/pre_filter.py`** — выбор словаря region-aware: `asia → keywords_asia`,
   иначе прежняя ветка по языку.
4. **`src/processors/enricher.py`** — ветка `region=="asia"`: `default_category=adtech-asia`,
   `default_geo=["АЗИЯ"]`, `author=news-asia`; `_region_hint` — asia-подсказка.
5. **`src/writers/news_writer.py`** — `_finalize_meta`: ветка `asia` (category/geo).
6. **`src/prompts/digest_writer.md`** — подсекция «Азия» при `Category: adtech-asia`
   (симметрично «Россия»).
7. **Тесты** — `tests/test_asia_pipeline.py` (зеркало `test_ru_pipeline.py`): скоринг
   `keywords_asia`, детерминизм региона в enricher/news_writer, выбор словаря asia в pre-filter.

### Критерии завершения

- Alizila в `sources.yaml` (`language: en`, `region: asia`, `max_age_days: 30`); `keywords.yaml` не изменён.
- `keywords_asia.yaml` создан и **выбирается по region в pre_filter** (не по языку).
- Азиатские материалы публикуются с `category: adtech-asia`, `author: news-asia`, `geo: ["АЗИЯ"]`
  (проверено в enricher **и** news_writer).
- Западный + азиатский источник одного события → 1 статья (мультисорс работает; region=primary — осознанно).
- Дайджест секционирует «Азия». Тесты зелёные.
- Проверка боем (тест-режим воркфлоу) — Alizila отвечает без 403, объём в норме (max_age работает).

Оценка: 3–4 ч (правки мелкие, по образцу RU).

## Бэклог: Update-flow (Фаза 2 дедупа) — междневное тихое обновление статьи

Вынесено из ТЗ дедупа отдельной задачей (решение владельца 17.07). Суть: новый
источник по УЖЕ опубликованной теме → текст существующей статьи обновляется новыми
фактами, без видимой пометки для читателя (`updatedDate` во front-matter для SEO —
Яндекс.Новости/Google News). Самый рискованный кусок (меняем live-контент,
кросс-репо, новые LLM-вызовы) — потому Фазой 2, на уже подтверждённом боем ядре.

Компоненты (из ТЗ, разделы 7–8):
- **Обнаружение:** на шаге дедупа translated_item против опубликованного,
  `max_sim > UPDATE_THRESHOLD` (0.65, ниже порога дубля) → кандидат на обновление.
- **LLM-анализ новых фактов** (`src/prompts/update_check.md`, GLM-Flash): есть ли в
  новом источнике факты, которых нет в опубликованной статье; major vs minor.
- **Маршрутизация:** major → **Editor** (`src/processors/update_editor.py`,
  `update_editor.md`, DeepSeek-V3) переписывает тело с новыми фактами; minor →
  add-source-only (только `additional_sources` += новый + `updatedDate = now`).
- **QA-расширение:** проверка на ПОТЕРЮ фактов при переписывании (ключевые цифры
  старой версии должны остаться) + валидность `updatedDate`.
- **Publisher update-mode** (`git_publisher.update_article`): обновляет `.md` в
  `media` (нужен доступ к телу — либо `body` в `published.jsonl`, либо читать
  `.md` из репо `media`), `published.jsonl`: `updated_at`, `update_history[]`.
- **Порядок:** сначала кросс-репо схема (`updatedDate` уже в FIELD_ORDER — на
  сайте добавить рендер/JSON-LD), потом detection+routing+editor, потом publisher.
- **Риски:** ложный апдейт (перепишем не ту статью при пороге 0.65) — начать с
  add-source-only (дёшево/обратимо), major-rewrite включать после проверки боем.

## Сессия 13.07.2026 (СМЁРЖЕНО): задача 4 «ИИ-голос» — промпт + хирургическая петля + тест-режим

Ветка `claude/ai-voice-task-solution-bfyq4v` → **смёржена в `main`** (PR по задаче 4).
Цель — поднять выход статей: QA бракует ~74% как «ИИ-голос», раньше это =
безвозвратная потеря материала (поток `write→enrich→qa` линейный, отказ →
`failed/`, ноль попыток исправить). **Результат тест-прогона (владелец):
«стало немного лучше».** Точечные дефекты петля чинит; диффузный ИИ-голос —
нет (→ задача 4a, образцы).

**Сделано и смёржено:**
- **B. Промпт писателя (`news_writer.md`):** жёсткий обязательный блок «что это
  значит для рекламодателей» заменён гибкой структурой — именно одинаковая
  «выводная» концовка выдавала машинный текст (промпт форсировал шаблон и сам же
  его запрещал). Добавлена позитивная модель голоса.
- **A. Хирургическая петля ревизии (`stage_write` + `revise_news` + `reviser.md`):**
  при отказе QA по стилю/тону/фактам (не rules) — ОДИН проход ревизии. Модель
  получает СВОЙ прошлый текст + процитированные QA-замечания и правит ТОЛЬКО
  помеченные места (температура 0.3), остальное дословно. Это осознанно НЕ
  blanket-rewrite: «перепиши заново» дал бы тот же ИИ-голос в новой обёртке —
  правим точечные дефекты. В `failed/` уходит только не прошедшее и после ревизии.
  Метрика `qa_recovered` в логе. Петля жёстко ограничена одним проходом на событие.
- **C. QA (`qa_style.md` + `qa.py`):** планка сохранена (строгий фильтр), но
  `issues` теперь обязаны быть конкретными (цитата + способ правки) — они питают
  петлю. `QAResult` несёт `llm_issues` и `retryable_style` (True, когда rules
  прошли, а завернул стиль).
- **Тест-режим воркфлоу** (галочка `test_mode` в `News Pipeline` → Run workflow) —
  безопасный прогон с ветки без побочек. См. секцию «грабли» в `CLAUDE.md`.
  env `PIPELINE_TEST_MODE`: dry-run (без PR/состояния) + обход дедупа (иначе ветка
  наследует `seen_urls` из `main` и собирает 0) + окно свежести `TEST_MAX_AGE_DAYS=3`
  на все фиды + кап `TEST_MAX_ITEMS=15` в перевод/написание (стоимость и 45-мин
  лимит ограничены) + выгрузка `data/drafts/` артефактом. Реализация:
  `rss_collector`, `pre_filter`, `run_pipeline`, `news-pipeline.yml`;
  тесты `tests/test_test_mode.py`.
- Тесты: `tests/test_content_feedback.py` (ревизор, флаги QAResult, оркестрация
  петли), `tests/test_test_mode.py` (кап объёма). LLM-логика локально не
  тестируется — валидация тест-прогоном воркфлоу.

**Тайминг:** петля добавляет ≤1 доп. цикл write+enrich+qa на забракованный по
стилю материал. На объёме run #15 (~35 событий) worst-case +7–12 мин, итог
~30–35 мин при потолке 45 — в таймаут не упираемся. Страховочный кап на число
ревизий обсуждался, владелец отложил.

## Задача 4a (ГЛАВНОЕ на следующую сессию): образцы качественных формулировок для писателя

Тест-прогон показал: петля ревизии вытягивает **точечные** дефекты, но **диффузный
ИИ-голос** (весь текст ровно-гладкий, ни одной фразы не ткнёшь) она не лечит — и не
может, это природа генератора. Владелец: *«реально нужно работать над примерами
удачных и качественных формулировок, чтобы обогащать навык агента писателя».*

**Суть задачи:** поднять качество **первого прохода** через few-shot — показать
модели «как надо», а не только запрещать «как не надо» (запреты против диффузного
голоса не работают). Это главный рычаг, петля ревизии — лишь подстраховка.

**План (курирует владелец — что считать эталоном):**
- Скурировать **корпус эталонных формулировок**: удачные лиды, переходы, концовки,
  живые абзацы — из опубликованных статей (`media`, `media-site/src/content/news/`)
  и/или отобранных владельцем текстов. ⚠️ Не «любая опубликованная = эталон».
- Вшить в `news_writer.md` как **few-shot якорь стиля** (1–2 полных образца и/или
  набор пар «слабо → сильно»). Возможно — вынести в отдельный файл примеров и
  подгружать, чтобы не раздувать промпт.
- Прогнать тест-режимом, сравнить голос до/после на артефакте черновиков.
- Параллельно (опц., решение по стоимости) — **потолок качества голоса**: сильнее
  модель на этап письма (в `models.yaml` застаблен `anthropic`; есть
  `deepseek-reasoner`).
- Опц. корпус брака (сохранять `drafts/failed/` + вердикт QA) для тюнинга — теперь
  дешевле, т.к. тест-режим уже выгружает черновики артефактом.

Кандидатов в эталоны на старте сессии можно вытащить из репо `media` (add_repo)
и показать владельцу на выбор.

## Итог сессии 13.07.2026: дешёвые баги заголовков (задачи 5, 6) + персист (задача 1)

**Смёржено в `main`:**
- **Задача 1 (персист состояния) — PR #16.** `permissions: contents: write` в `news-pipeline.yml`/`digest.yml`; «Commit data state» больше не падает с 403. Настройку репо менять не пришлось.
- **Задачи 5 и 6 — PR #18.** Обе дешёвые, детерминированный код (не LLM-логика), проверены юнит-тестами локально.
  - **Задача 5 (двоеточие в заголовке):** `split_front_matter` (`src/utils/frontmatter.py`) на `yaml.YAMLError` запускает `_repair_front_matter` — квотит незакавыченные значения строковых полей (`title`/`description`/`social_title`/`pubDate`) и парсит повторно. Валидный front-matter не затрагивается. Материал больше не теряется целиком из-за одного `:`.
  - **Задача 6 (длина заголовка вне 50–80):** `write_news` (`src/writers/news_writer.py`) при промахе делает **один** авто-ретрай с хинтом (`_retry_hint`); результат берётся только если попал в диапазон, иначе остаётся первый вариант. Промпт `news_writer.md` усилен.
  - Тесты: `tests/test_content_feedback.py` (+12).

**Эффект трезво:** обе задачи закрывают ~2–3 отказа из 26 за прогон (по раскладке run #15). Основной массив отказов — «ИИ-голос» (**задача 4**) — не затронут. Следующий крупный рычаг для роста выхода статей — именно задача 4.

**Что дальше (по приоритету):** №2 (дедуп), №3 (дайджест на своих материалах), №4 (обучение качества текстов) — все разблокированы персистом (№1). Проверить, что после планового прогона `data/state/` реально коммитится в `main` (коммит `state: pipeline run …`) и дубли перестают протекать.

## Итог сессии 12.07.2026: российские источники (Phase 3a) + фикс коллектора

**Сделано и смёржено в `main` (PR #10 — RU-источники, PR #11 — фикс коллектора):**
- **RU-источники в едином пайплайне** (не параллельном): 3 RSS-фида (Яндекс Реклама, AdRiver, АКАР) в `sources.yaml` с `language: ru`/`region: ru`. Публикуются от **`news-ru`** («Служба новостей Россия»).
- **`keywords_ru.yaml`** — русский словарь пре-фильтра (порог L1=10 против 20 у англ.); `pre_filter.py` выбирает словарь по языку item.
- **Enricher region-осведомлён** — для `region==ru` детерминированно `adtech-ru`/`geo ["РФ"]`/автор `news-ru`. Тесты `tests/test_ru_pipeline.py`.
- **Дайджест** — секция «Россия» при наличии `adtech-ru`.
- **Фикс зависона коллектора** (первый прогон run #13 висел 38 мин на Яндексе → отмена по таймауту): `_parse_feed` качает фид через `requests` с таймаутом 20 с; **пофидовый** фильтр свежести `max_age_days: 30` только у RU-фидов (мировые — без ограничения); кап 120/фид; лог по каждому фиду. Тесты `tests/test_collector.py`.

**Проверено боем (run #15):** Яндекс ответил за ~28 с (не завис), собрано 128 (не 1620), 23 мин, $0.14, **9 опубликовано (5 мир + 4 России)**, PR в `media` создан. RU-разметка корректна.

**⚠️ Главное на СЛЕДУЮЩУЮ сессию (владелец берёт задачу 7):**
- **Задача 7 (персист состояния) — теперь прямой блокер RU-инкремента.** `seen_urls.json` и `published.jsonl` не сохраняются между прогонами (шаг «Commit data state» падает: на `main` — 403 у дефолтного токена; с фиче-ветки — add/add-конфликт rebase). Следствие: **каждый прогон = «первая загрузка»** (заново тащит 30-дневное окно RU), а `published.jsonl`-дедуп пуст → **риск повторной публикации** уже вышедших материалов. Фильтр 30 дней это НЕ решает (он только режет архивный хвост). Поведение «первая загрузка → дальше только свежее» включится только после фикса задачи 7. Решение: `permissions: contents: write` в `news-pipeline.yml`/`digest.yml` + Settings→Actions→«Read and write», ЛИБО push состояния через PAT. Готовый блок `permissions` можно добавить в воркфлоу заодно.
- **Качество текстов (задача 3) — обострилось.** На run #15 QA забраковал 26/35 (~74%), почти всё «ИИ-голос», одинаково у мира и РФ. Центральная докрутка — `src/prompts/news_writer.md`. RU-источники пресс-релизные → особенно уязвимы.
- **Мелочи:** длина заголовка вне 50–80 (задача 5, 2 отказа на run #15); NRF-событие с RU-фида ошибочно помечено `adtech-ru` (edge-case «RU-источник про мировое событие» — при желании фильтровать по гео-содержанию, а не только по региону фида).

## Итог сессии 10.07.2026

- **Задача 1b (`pubDate`) закрыта.** `pubDate` теперь = момент выхода на 1screen (`utcnow_iso()`), а не дата источника из RSS. Детерминированная доводка front-matter вынесена в `news_writer._finalize_meta`; дата источника сохраняется отдельно в `published.jsonl` (`source_published_at`, `git_publisher`). Промпт `news_writer.md` → `pubDate: "AUTO"`. Тесты добавлены (`tests/test_content_feedback.py`, `test_pubdate_*`). PR смёржен владельцем.
- **Правка forward-only** — уже опубликованные материалы не трогаются, эффект на следующей партии. Владелец **не запускал** ручной прогон: проверка отложена до планового батча.
- **Осталось на очереди (по важности):** задача 7 (403 на «Commit data state» — состояние дедупа не сохраняется, корневой инфра-баг), задача 1 (сверка «отправлено ↔ на сайте»), задача 6 (дедуп-дубли), баги заголовков 4/5, качество текстов 3, корпус good/bad 2.

## Итог сессии 09.07.2026

- **Все 7 секретов добавлены** владельцем и проверены боем (workflow «Проверка ключей»): `ZHIPU_API_KEY`, `ZHIPU_BASE_URL`, `GLM_FLASH_MODEL`, `DEEPSEEK_API_KEY`, `GH_TOKEN`, `MEDIA_REPO`, `MEDIA_SITE_SUBDIR`. GLM (`glm-4.5-flash` на z.ai) и DeepSeek (`deepseek-chat`) отвечают. Эмбеддинги z.ai не отдаёт — семантический дедуп отключён (by design).
- **Найден и исправлен ключевой баг GLM.** `glm-4.5-flash` на z.ai по умолчанию включает reasoning («thinking») → тратит `max_tokens` на рассуждение и возвращает пустой `content` → JSON-этапы (pre_filter, enricher, qa, filter_dedup) падали на `json.loads("")` и уходили в детерминированный фолбэк, а сами вызовы шли ~15-20 сек вхолостую (из-за чего прогон упирался в 30-мин тайм-аут). Фикс: слать `extra_body={"thinking":{"type":"disabled"}}` для GLM-вызовов (`src/llm_client.py`). Тайм-аут поднят 30→45 мин.
- **Первый успешный прогон:** 100 собрано → 21 pre-filter → 21 переведено → 10 прошло QA → **9 опубликовано** (1 дубль убран вручную). Стоимость $0.079, время 14 мин. PR #9 в `media` **смёржен на сайт** (коммит `34f25de`).
- **Фикс GLM влит в `main`** (PR #1 смёржен) — плановые прогоны с `main` идут на рабочем коде. Также в `main` добавлены `CLAUDE.md` обоих репозиториев (PR #2 здесь, PR #10 в `media`).
- **Статьи подтверждены на сайте.** Была заминка «не вижу статей» — оказалось **кэш/задержка деплоя Timeweb**; после обновления все 9 материалов на месте (`/news` + главная). Не баг.

## Задача 1: сверка «отправлено ↔ на сайте»

После каждого прогона проверять, что все опубликованные материалы реально доехали на сайт. Источник «отправлено» — `data/published/published.jsonl` (или список файлов в PR); «на сайте» — коллекция `media-site/src/content/news/` в `main` репо `media` (в идеале — и живой `https://1screen.ru/news`). Автоматизировать: скрипт/шаг, который сверяет slug'и опубликованных с файлами в `media` и репортит расхождения. Контекст: в этой сессии «статей не видно» оказалось кэшем Timeweb (см. заметку про верстку) — но явная сверка нужна, чтобы такие вопросы закрывались автоматически, а не глазами.

## Задача 1b (баг, ✅ ИСПРАВЛЕНО 10.07.2026): `pubDate` = дата публикации на 1screen, а не источника

**Было:** `src/writers/news_writer.py` ставил `meta["pubDate"] = event["published_at"]` — дату материала **в источнике** (из RSS). Из-за этого на сайте статьи датировались задним числом (напр. 07-08 вместо реальной публикации 07-09): выглядели несвежими, не зажигали live-точку на главной (`isLive` = моложе 3 ч), а батчи со старыми датами источника **тонули внизу ленты** (сортировка по `pubDate` убыв.).

**Фикс:** детерминированная доводка front-matter вынесена в `news_writer._finalize_meta`, где `pubDate = utcnow_iso()` (момент выхода на 1screen). Дата источника не теряется — паблишер пишет её в `published.jsonl` отдельным полем `source_published_at` (`src/publishers/git_publisher.py`); во front-matter она не идёт, чтобы не расширять контракт схемы сайта. QA-проверка «pubDate в будущем» проходит (дата всегда в прошлом). Промпт `news_writer.md` — `pubDate: "AUTO"` (значение всё равно перетирается кодом). Тесты: `tests/test_content_feedback.py` (`test_pubdate_is_now_not_source_date`, `test_pubdate_not_in_future_for_qa`, `test_finalize_sets_source_and_defaults`).

## Заметка: лимиты вывода в верстке (гипотеза про «не видно статей»)

В этой сессии «не видно статей» **разрешилось само после обновления кэша Timeweb** — материалы на месте. Проверено по коду `media-site`: рубрика `/news` (`layouts/SectionPage.astro`) выводит **все** материалы без лимита (сортировка по `pubDate` убыв.). Главная (`pages/index.astro`) намеренно лимитирует: hero (1) + «Главное сейчас» (3, live-точка <3 ч) + «Свежее» (6) + «Популярное» (5) + «Мнения» (2) — by design. На будущее: при «не видно» сперва кэш/`/news`, лишь потом сборку. **Связано с задачей 1b:** пока `pubDate` = дата источника, свежие материалы не попадают в «Главное сейчас» и сортируются ниже — это стоит починить в первую очередь.

## Задача 2: механизм корпуса «качественные / некачественные»

Владельцу нужно докручивать агента написания на реальных примерах. Сейчас:
- **хороший корпус** = опубликованные статьи (в репо `media`, `media-site/src/content/news/`);
- **плохой корпус** = забракованные QA черновики уходят в `drafts/failed/` **на эфемерном раннере и теряются** — их не видно.

Надо **сохранять забракованные черновики + вердикт QA (с причиной)** между прогонами: либо коммитить `data/drafts/failed/` в репо, либо загружать как artifact воркфлоу (`actions/upload-artifact`). Тогда у владельца будет размеченный набор «прошло / не прошло + почему» для тюнинга промптов. Причины QA уже пишутся в лог — можно дополнительно собирать их в сводный отчёт (slug → вердикт → причина).

## Задача 3 (отдельная, для владельца): промпты и качество текстов

Главный сигнал первого прогона: **7 из 11 отказов QA — это «ИИ-голос».** GLM-QA стабильно бракует тексты формулировкой вроде *«слишком гладкий, безликий и шаблонный, нет авторской позиции»*. Это центральная цель тюнинга `src/prompts/news_writer.md`: живее, авторская интонация, меньше предсказуемых подзаголовков/шаблонных абзацев. Параллельно — выверить, не слишком ли строг сам QA-порог (`src/prompts/qa_style.md` / пороги в `src/processors/qa.py`).

**Раскладка первого прогона (21 материал) для старта:**

Прошли QA и опубликованы (9): Adobe Custom Algorithms · Walmart+Magnite CTV · DuckDuckGo/YouTube · Paramount видеоданные · Meta Advantage+ Muse · TikTok 250-летие · Meta $1.4 трлн · Google Search Console соцсети · Meta детская реклама.

Забраковано QA (11):
- **«ИИ-голос» / стиль (7):** izdateli-google-poisk, markologi-ii-influence, sky-itv-deal, dentsu-meta-infrastruktura, otravlenie-ii, ace-hardware-redvest, prevratil-tv-reklamu → **основной материал для докрутки промпта**.
- **Длина заголовка (3):** vnimanie (45 симв., коротко), strategus (91, длинно), nju-dzherzi (44, коротко) — нужно 50-80. См. задачу 5.
- **Ошибка обработки (1):** «рекламный пилот в Европу: …» — двоеточие в заголовке сломало YAML front-matter. См. задачу 4.

## Задача 4/5 (баг, ✅ ЗАКРЫТО 13.07.2026): двоеточие в заголовке ломает front-matter

Один материал упал: `mapping values are not allowed here` — DeepSeek поставил `:` в значение YAML без кавычек, весь материал терялся. **Фикс:** `split_front_matter` (`src/utils/frontmatter.py`) обёрнут в try — при `yaml.YAMLError` запускается `_repair_front_matter`, который квотит незакавыченные значения строковых полей (`title`, `description`, `social_title`, `pubDate`) и парсит YAML повторно. Штатный (валидный) front-matter не затрагивается — починка срабатывает только на ошибке. Промпт `news_writer.md` дополнительно просит ставить двоеточие только внутри кавычек. Тесты: `tests/test_content_feedback.py` (`test_unquoted_colon_*`, `test_valid_front_matter_unchanged_by_repair`).

## Задача 5/6 (баг, ✅ ЗАКРЫТО 13.07.2026): длина заголовка вне 50-80

`news_writer` выдавал заголовки 44/45/91 симв. → бракуются QA. **Фикс:** `write_news` (`src/writers/news_writer.py`) после генерации проверяет длину `title` (`_title_len_ok`, диапазон синхронен с `qa.py`) и при промахе делает **один** авто-ретрай с корректирующим хинтом (`_retry_hint`: короче/длиннее + сам заголовок). Результат ретрая берётся только если он попал в диапазон — иначе остаётся первый вариант (QA разберётся), чтобы не тратить ещё вызовы. Промпт `news_writer.md` усилен («СТРОГО 50–80 символов, считай знаки с пробелами»). Тесты: `tests/test_content_feedback.py` (`test_title_len_ok_boundaries`, `test_retry_hint_direction`, `test_write_news_*`).

## Задача 6: дедуп-дубли без эмбеддингов

Без семантического дедупа одна новость из двух RSS-фидов даёт 2 статьи (в этом прогоне поймали DuckDuckGo руками — обе ссылались на статью 416389 MediaPost, но пришли из фидов TV и Media). Варианты: URL-канонизация (нормализовать/обрезать до article-id), дедуп по заголовку (fuzzy), либо отдельный провайдер эмбеддингов (bigmodel-ключ или другой).

## Задача 7 (БАГ, ✅ ЗАКРЫТО 13.07.2026): «Commit data state» падал с 403 → состояние дедупа не сохранялось

**Решено (PR #16):** в `news-pipeline.yml` и `digest.yml` добавлен блок `permissions: contents: write`. Оказалось достаточно **только** этого — настройку репо (Settings → Actions → Workflow permissions) владелец не трогал, дефолт токена остался read-only, но блок `permissions:` на уровне воркфлоу его переопределяет. Проверено боем: digest run 13.07 — «Commit data state» зелёный, `git push` без 403. Вариант с PAT (ниже) не понадобился. Ниже — исходный разбор, оставлен для истории.

Финальный шаг воркфлоу `git push` падает с **403 `Permission to …/media-agents.git denied to github-actions[bot]`** — у дефолтного `GITHUB_TOKEN` нет прав на запись (в `news-pipeline.yml` и `digest.yml` нет блока `permissions`, а в настройках репо Actions, судя по всему, стоят read-only). Job краснеет уже **после** создания PR — PR-ы идут через отдельный PAT `GH_TOKEN`, поэтому проходят, а вот коммит состояния пушится дефолтным токеном и отлетает.

**Прошлый диагноз был неверен.** Дело НЕ в add/add-конфликте `git pull --rebase origin main` на фиче-ветке. Проверено на боевом прогоне 09.07 (run `29008723017`, ветка **`main`**): rebase прошёл чисто (`Current branch main is up to date`), упал именно `git push` с 403. Значит и на `main` шаг НЕ проходит — `data/state/` и `data/published/` не коммитятся **никогда**.

**Почему это важно (а не косметика):**
- `data/state/seen_urls.json` и `translation_cache.json` не сохраняются → память дедупа обнуляется каждый прогон → прямой вклад в дубли (**Задача 6**; 09.07 поймали двойной DuckDuckGo).
- `data/published/published.jsonl` не сохраняется → ломает сверку «отправлено ↔ на сайте» (**Задача 1**).
- Каждый прогон помечается красным, хотя контент и PR делаются — маскирует настоящие падения.

**Решение (достаточно любого варианта):**
1. **Дать воркфлоу права на запись (проще, предпочтительно).** Добавить в `news-pipeline.yml` и `digest.yml`:
   ```yaml
   permissions:
     contents: write
   ```
   ~~и в Settings → Actions → General → Workflow permissions выставить «Read and write permissions».~~ (Не понадобилось — блока `permissions:` в воркфлоу достаточно, репо-настройку не меняли.)
2. **Либо пушить состояние через PAT.** В шаге «Commit data state» пушить не дефолтным токеном, а токеном с write-доступом на `media-agents` (`git push https://x-access-token:${TOKEN}@github.com/<owner>/media-agents`). Учесть: текущий `GH_TOKEN` заведён под создание PR в репо `media` — для этого варианта у него должен быть `repo`-скоуп и на `media-agents`.

После фикса убедиться, что `data/state/` реально обновляется в `main` после прогона (по коммиту `state: pipeline run …`), и что дубли из Задачи 6 перестают протекать между прогонами.

> ⚠️ Тот же устаревший диагноз («add/add-конфликт с фиче-ветки») продублирован в `CLAUDE.md` (раздел «Подводные камни») — при фиксе поправить и там.

## Полезные команды (локальная отладка)

```bash
python scripts/check_keys.py          # диагностика ключей (endpoint/модели)
python run_pipeline.py --dry-run      # прогон без git-операций, черновики в data/drafts/
python run_pipeline.py --stage collect   # один этап
python scripts/cost_report.py         # расходы LLM
pytest                                # тесты
```

## Известное / вне MVP

- Пресс-релизы/блоги/Азия, review/report, Telegram-алерты, эскалация на Claude — следующие итерации (ТЗ, раздел 1.1).
- Старый GLM-ключ `d57ac7c8…` мог засветиться в чате прошлых сессий — проверить, что в секретах боевой (новый).

## Реализация Базы знаний (Spravochnik) — Фаза 1 backend (19.07.2026)

**Статус: backend реализован в media-agents, тесты зелёные (19 новых). Боем НЕ проверено
(нужен прогон `Spravochnik Pipeline` → Run workflow, dry_run). Site-часть (Фаза 2) —
в репо `media`, ещё не начата.** ТЗ: `для_кодинга/ТЗ_База_знаний.md`.

**Сделано (media-agents):**
- Модуль `src/spravochnik/`: `queue_manager` (очередь + lifecycle + sync merged PR),
  `researcher` (Wikipedia с normalize+timeout, related по живой коллекции сайта),
  `writer` (DeepSeek + сноска РКН `legal.add_restricted_org_footnotes` + finalize-гейт,
  JSON-LD НЕ пишет), `fact_checker` (GLM, best-effort, без Wiki → не блокирует),
  `qa` (rules уровень 1: схема/facts/разделы/описание≤160/CJK-мусор/thin/slug), `publisher`
  (reuse `media_repo`, draft-PR), `run` (оркестрация + sync).
- `config/spravochnik_queue.yaml` (8 стартовых айтемов, приоритет 1).
- Промпты `spravochnik_writer.md` / `spravochnik_fact_checker.md`.
- `models.yaml`: стадии `spravochnik_writer: deepseek:chat`, `spravochnik_fact_checker: glm:flash`.
- Workflow `.github/workflows/spravochnik.yml` (cron 6ч + dispatch, `permissions: contents: write`,
  «Commit queue state»). timeout 30 мин.
- Тесты `tests/test_spravochnik.py` (19, без сети/LLM). Полный прогон: 118 passed (6 падений —
  предустановочные: numpy/sklearn для `test_tfidf` не встал в песочнице, не связано).

**Осталось / развилки перед боем:**
- **`author` для справочника — РЕШЕНО (владелец 19.07): поле НЕ используется.** Коллекция
  `spravochnik` без `author`; backend автора не эмитит (writer его не проставляет). На сайте
  не добавлять `author: reference('authors')` в схему `spravochnik`.
- **Фаза 2 (site, репо `media`, отдельный PR, нужен `add_repo media`):** коллекция `spravochnik`
  (`z.discriminatedUnion` по type ИЛИ суперсет optional), роуты `/spravochnik/`+`/spravochnik/[slug]`,
  **JSON-LD генерится в Astro из `facts`+`type`** (не из front-matter!), `BreadcrumbList`, render-time
  фильтр `related` (иначе 404), `SpravochnikLayout`/виджет/nav, OG, секция в `llms.txt`. Раздел 12 ТЗ.
- **Секреты:** те же, что у новостей (DEEPSEEK/ZHIPU/GH_TOKEN/MEDIA_REPO/MEDIA_SITE_SUBDIR) — уже
  в репо, доп. секретов нет.
- **Первый прогон:** `Spravochnik Pipeline` → Run workflow с `dry_run=true` — проверить, что
  Wikipedia отвечает из Actions, writer выдаёт валидный front-matter, rules-QA не бракует всё.
  ⚠️ Локально LLM/Wiki-часть не тестируются (ключи в Secrets, egress песочницы режет часть хостов).
